name: Mod Build

on: [ workflow_dispatch, push ]

jobs:
  mod_build:
    permissions:
      contents: read
      packages: write
    uses: P3pp3rF1y/MultiWorkspace/.github/workflows/mod-build-template.yml@26.1
    secrets: inherit